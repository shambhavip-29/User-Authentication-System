# User-Authentication-System
This project is a Java-based User Authentication System that supports secure user registration, login, and password reset using JDBC with MySQL. It validates inputs, prevents duplicate email registration, and stores passwords safely using SHA-256 hashing.
